import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Render the 'App' component into the HTML div element with the id 'root'.
ReactDOM.render(
    <App />, document.getElementById("root")
);

